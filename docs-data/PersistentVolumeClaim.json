{
	"type": "PersistentVolumeClaim",
	"descriptiveType": "Persistent Volume Claim",
	"getDescription": "Get a persistent volume claim by name",
	"listDescription": "List all persistent volume claims",
	"searchDescription": "Search for a persistent volume claim using the ListOptions JSON format: https://github.com/overmindtech/k8s-source#search",
	"group": "Kubernetes",
	"terraformQuery": [
		"kubernetes_persistent_volume_claim.metadata[0].name",
		"kubernetes_persistent_volume_claim_v1.metadata[0].name"
	],
	"terraformMethod": "GET",
	"terraformScope": "${provider_mapping.cluster_name}.${values.metadata[0].namespace}",
	"links": [
		"PersistentVolume"
	]
}