{
	"type": "Pod",
	"descriptiveType": "Pod",
	"getDescription": "Get a pod by name",
	"listDescription": "List all pods",
	"searchDescription": "Search for a pod using the ListOptions JSON format: https://github.com/overmindtech/k8s-source#search",
	"group": "Kubernetes",
	"terraformQuery": [
		"kubernetes_pod.metadata[0].name",
		"kubernetes_pod_v1.metadata[0].name"
	],
	"terraformMethod": "GET",
	"terraformScope": "${provider_mapping.cluster_name}.${values.metadata[0].namespace}",
	"links": [
		"ConfigMap",
		"PersistentVolumeClaim",
		"PriorityClass",
		"Secret",
		"ServiceAccount",
		"ip"
	]
}